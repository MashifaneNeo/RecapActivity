<UserControl x:Class="HolidayShowdown.Views.ResultView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" Background="{StaticResource BrandAccent}" CornerRadius="8" Margin="0,0,8,0" Padding="16">
                <TextBlock x:Name="LeftText" TextWrapping="Wrap" FontSize="14"/>
            </Border>
            <Border Grid.Column="1" Background="{StaticResource BrandAccent}" CornerRadius="8" Margin="8,0,0,0" Padding="16">
                <TextBlock x:Name="RightText" TextWrapping="Wrap" FontSize="14"/>
            </Border>
        </Grid>

        <TextBlock x:Name="VerdictText" Grid.Row="1" FontSize="22" FontWeight="Bold" Margin="0,20,0,0" HorizontalAlignment="Center"/>
        <TextBlock x:Name="CombinedText" Grid.Row="2" FontSize="14" Margin="0,8,0,0" HorizontalAlignment="Center" Foreground="#555"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
            <Button Content="⭐ Save winner to favourites" Click="Favourite_Click" Padding="10,6" Margin="0,0,8,0"/>
            <Button Content="Compare again" Click="Back_Click" Padding="10,6" Background="{StaticResource BrandPrimary}" Foreground="White"/>
        </StackPanel>
    </Grid>
</UserControl>